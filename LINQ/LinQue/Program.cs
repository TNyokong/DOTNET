// Specify the data source

 //List<int> scores = [87, 99, 97, 92, 81, 100];

// Define the query expression.
//  IEnumerable<int> scoreQuery = //Integrated into the language
//      from score in scores //Same as Quiry Language -from scores select score
//      where score > 80
//      orderby score descending
//      select score;

// List<int> myScores =scoreQuery.ToList();

// Execute the Query

//  foreach (int i in scoreQuery)
//  {
//      Console.Write(i + " ");
//  }

// foreach (string i in scoreQuery)
//  {
//      Console.WriteLine(i + " ");
//  } 

